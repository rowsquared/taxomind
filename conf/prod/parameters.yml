# Production parameter overrides.
# Loaded on top of conf/base when running with --env=prod.

embedding:
  cache_dir: null  # Optional Hugging Face cache directory
  local_files_only: false  # Disable network calls when true
  batch_size: 4  # Batch size for taxonomy/learning/enrich embeddings
