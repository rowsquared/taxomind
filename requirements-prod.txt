# Production requirements for Taxomind API
# Only what's needed to run the API and pipelines

# Core API framework
fastapi==0.121.3
uvicorn[standard]==0.38.0
pydantic==2.12.4

# Kedro pipeline execution (minimal)
kedro==1.0.0
kedro-datasets[pandas.csvdataset]==9.0.0

# ML/NLP libraries (needed by pipelines)
sentence-transformers==5.1.2
setfit==1.1.3
scikit-learn==1.7.2
torch==2.9.1
transformers==4.57.1

# Data processing
numpy==2.2.6
pandas==2.3.3

# Configuration
pyyaml==6.0.3

# Optional: Uncomment if using ollama in production
# ollama==0.6.1
